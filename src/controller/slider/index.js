const addSlider = require('./addSlider');
const getSliders = require('./getSliders');
const updateSlider = require('./updateSlider');
const deleteSlider = require('./deleteSlider');

module.exports = { addSlider, getSliders, updateSlider, deleteSlider };
