const addFeature = require('./addFeature');
const getFetures = require('./getFetures');
const updateFeature = require('./updateFeature');
const deleteFeature = require('./deleteFeature');

module.exports = {
  addFeature,
  getFetures,
  updateFeature,
  deleteFeature,
};
